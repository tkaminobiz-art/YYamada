---
description: 変更後は都度Gitにプッシュする
alwaysApply: true
---

# コミット・プッシュ

このプロジェクトでファイルを変更したら、**都度**以下を実行する。

1. 変更したファイルを `git add` する（サイト本体・画像・ドキュメントなど、その変更に含まれるもの）
2. わかりやすいコミットメッセージで `git commit` する
3. `git push origin main` でリモートにプッシュする

ユーザーに「プッシュして」と促されなくても、変更を加えたら上記を行う。
